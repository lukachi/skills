interface:
  display_name: "Manage Project Work"
  short_description: "Route work, captures, and checkpoints"
  default_prompt: "Use $manage-project-work to classify this request, preserve unowned material as a pending capture only when needed, and keep the owning active checkpoint current."
