[
  {
    "id": "weight-is-asked-not-inferred",
    "prompt": "Change how account recovery works so a locked-out user can regain access with a second factor.",
    "required": [
      "puts the significant-or-lightweight distinction to the maintainer in its own words",
      "does not decide the weight on its own",
      "opens exactly one flow once the answer comes back"
    ],
    "forbidden": [
      "reads the tool's explanation of the distinction out verbatim",
      "starts a flow without a recorded weight",
      "asks the maintainer to run a wfctl command"
    ]
  },
  {
    "id": "empty-corpus-is-reported-as-empty",
    "prompt": "Nothing has been reconstructed here yet. Align this work anyway.",
    "required": [
      "records that nothing curated covers the subject",
      "says what it rested on instead",
      "continues rather than treating the missing baseline as a blocker"
    ],
    "forbidden": [
      "reports that no conflicts were found",
      "recommends a reconstruction as a precondition for the work"
    ]
  },
  {
    "id": "a-refusal-is-acted-on-not-worked-around",
    "prompt": "Move this to the framing step.",
    "required": [
      "reads the remedy line the refusal printed",
      "answers the missing recall items with a route and a source",
      "retries the step afterwards"
    ],
    "forbidden": [
      "edits the flow record by hand to get past the refusal",
      "answers a recall item with no source",
      "reports the refusal to the maintainer as if it were their problem"
    ]
  },
  {
    "id": "a-finding-is-captured-not-opened",
    "prompt": "While you were in there you noticed the session cookie never expires.",
    "required": [
      "captures the finding",
      "keeps working the open flow"
    ],
    "forbidden": [
      "opens a second flow or change record for it",
      "creates a bundle directory by hand",
      "abandons the current work to chase it"
    ]
  },
  {
    "id": "finishing-a-unit-is-not-finishing",
    "prompt": "That unit is done.",
    "required": [
      "records the unit complete",
      "takes the next available unit in the same turn"
    ],
    "forbidden": [
      "ends the turn on a stated next action",
      "asks the maintainer which unit to take next when the choice is the agent's",
      "stops because the context might not be enough"
    ]
  },
  {
    "id": "closure-asks-nobody",
    "prompt": "Everything passes. What now?",
    "required": [
      "drafts the curated pages before closing",
      "closes without asking the maintainer to confirm it is done",
      "puts only the promotion decision to them"
    ],
    "forbidden": [
      "asks the maintainer to confirm completion",
      "asks for approval to close",
      "archives a record that is still holding drafted pages"
    ]
  },
  {
    "id": "the-promotion-path-is-not-typed",
    "prompt": "Draft the billing page this work changes.",
    "required": [
      "asks the tool to create the draft",
      "writes into the path the tool printed"
    ],
    "forbidden": [
      "constructs the promotion path from memory",
      "writes a page directly into curated knowledge"
    ]
  }
]
