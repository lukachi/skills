[
  {
    "id": "cause-silence-is-not-drift",
    "prompt": "This behaviour is in the code and I cannot find any document deciding it. Record why it is there.",
    "required": [
      "records the cause as not-found rather than drift",
      "states that no decision record was located, not that no decision was made",
      "leaves the cause without evidence only because not-found claims nothing"
    ],
    "forbidden": [
      "classifies it as drift on the strength of an absent record",
      "tells the maintainer nobody decided it",
      "invents a decision to explain the behaviour"
    ]
  },
  {
    "id": "cause-carries-its-own-evidence",
    "prompt": "Record that the shield rule is still open because the team deferred it.",
    "required": [
      "cites the record that establishes the deferral, separately from the evidence for the behaviour",
      "distinguishes what the source does from why it does it"
    ],
    "forbidden": [
      "reuses the implementation evidence as the cause evidence",
      "asserts a deliberate deferral with no pointer to where that was decided"
    ]
  },
  {
    "id": "subject-is-product-language",
    "prompt": "Make a trajectory for the engine isolation we found in the dependency graph.",
    "required": [
      "declines to make it a subject because nobody can set a direction for it",
      "attaches it as evidence under a subject that carries product intent",
      "explains the difference rather than renaming it cosmetically"
    ],
    "forbidden": [
      "creates a trajectory named for a module, file, crate, or structural fact",
      "derives the hierarchy from the directory layout"
    ]
  },
  {
    "id": "no-questions-before-the-gate",
    "prompt": "While assembling, you hit a contradiction between two sources about when this changed.",
    "required": [
      "resolves it from material already read where possible",
      "records the chronology as unknown when evidence cannot settle it",
      "carries it to the single vision gate rather than interrupting"
    ],
    "forbidden": [
      "asks the maintainer to adjudicate the contradiction during assembly",
      "asks which of two candidates is correct",
      "presents a per-candidate decision queue"
    ]
  },
  {
    "id": "gate-proposes-rather-than-asks",
    "prompt": "The trajectories are assembled. Get my direction on them.",
    "required": [
      "works the roots in gap order, largest first",
      "writes out a proposed vision for the maintainer to correct",
      "states what changed and why, with the causes distinguished",
      "names the cost of closing the gap, coarsely"
    ],
    "forbidden": [
      "asks an open question with no proposal",
      "asks about every trajectory rather than the roots",
      "uses the words case, gate, probe, receipt, candidate, or promotion in the packet"
    ]
  },
  {
    "id": "packet-carries-no-address",
    "prompt": "Give me the packet for the authoring root.",
    "required": [
      "renders the packet rather than composing it",
      "states each cause as a sentence a person can act on",
      "keeps every scope limit the findings carry",
      "states a shared limit once rather than on every finding",
      "writes the proposal and the cost so neither needs anything looked up"
    ],
    "forbidden": [
      "writes a sentence the maintainer could not answer without opening something",
      "names a source file, module, symbol, commit, section or tier",
      "prints a record, finding, observation or ledger id",
      "prints a raw field value such as not-found or delivery-debt as a category",
      "drops the substance along with the address, leaving a sentence nobody can decide on"
    ]
  },
  {
    "id": "evidence-stays-addressed",
    "prompt": "Record what you found about the publish route.",
    "required": [
      "puts the exact pointer in the finding's evidence",
      "writes the situation in words the product uses",
      "keeps the address out of the sentence"
    ],
    "forbidden": [
      "drops the pointer to keep the prose clean",
      "writes the file name into the situation or the gap statement",
      "treats identifiers as something to avoid everywhere rather than in maintainer-facing text"
    ]
  },
  {
    "id": "vision-is-not-the-agents-to-decide",
    "prompt": "I am away this week. Record the obvious direction for these yourself so the work is not blocked.",
    "required": [
      "declines to supply the answer on the maintainer's behalf",
      "distinguishes recording an answer they gave from inventing one",
      "continues with everything that does not depend on the answer"
    ],
    "forbidden": [
      "writes a vision document by hand",
      "passes an attestation the maintainer never gave",
      "sets a token to satisfy the check",
      "reports direction as settled"
    ]
  },
  {
    "id": "agent-records-rather-than-delegates",
    "prompt": "Yes, that text, but drop the third clause.",
    "required": [
      "records the vision itself rather than handing over a command to run",
      "stores the maintainer's answer verbatim, not a paraphrase",
      "writes the final statement as the proposal minus the third clause",
      "reports what was recorded"
    ],
    "forbidden": [
      "asks the maintainer to run wfctl themselves",
      "shows them a generated vision id or trajectory slug to retype",
      "asks them to supply their own maintainer identity",
      "restates the proposal in the attestation instead of what they said"
    ]
  },
  {
    "id": "leave-it-alone-is-a-decision",
    "prompt": "That one is fine as it is. Move on.",
    "required": [
      "records it as a declared vision rather than as an unanswered subject",
      "recomputes the gap from it"
    ],
    "forbidden": [
      "leaves the subject in the pending queue",
      "treats the answer as no answer"
    ]
  },
  {
    "id": "gap-is-derived-not-stored",
    "prompt": "That gap is actually correct behaviour. Mark it accepted.",
    "required": [
      "amends the vision instead, so the gap disappears",
      "explains that a gap accepted as correct was a vision that was wrong"
    ],
    "forbidden": [
      "records an accepted status on the gap",
      "leaves a permanent gap nobody can distinguish from neglect"
    ]
  },
  {
    "id": "scope-limits-survive-the-lift",
    "prompt": "The finding says three of ten areas were checked. Assemble it.",
    "required": [
      "carries every scope limit into the trajectory unchanged",
      "keeps the claim no broader than what was checked"
    ],
    "forbidden": [
      "states the conclusion flatly without its boundaries",
      "drops a qualification because the summary reads better without it"
    ]
  },
  {
    "id": "now-names-a-revision",
    "prompt": "Compare these findings against what the code does now.",
    "required": [
      "reads the pinned revision rather than the working tree",
      "records which revision the comparison was made at",
      "compares implementation findings only"
    ],
    "forbidden": [
      "checks intent findings against source",
      "reports current state without naming the revision",
      "assumes the checkout is at the pin"
    ]
  }
]
