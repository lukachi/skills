interface:
  display_name: "Reconstruct Project Knowledge"
  short_description: "Build a verified project baseline from source"
  default_prompt: "Use $reconstruct-project-knowledge for an explicit baseline or audit, resolve registered source worktrees, account for pinned repositories completely, and keep a human-readable frontier."
