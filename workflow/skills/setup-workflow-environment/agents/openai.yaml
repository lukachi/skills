interface:
  display_name: "Setup Workflow Environment"
  short_description: "Install and repair project workflow"
  default_prompt: "Use $setup-workflow-environment to install or safely update the workflow environment in this repository."
