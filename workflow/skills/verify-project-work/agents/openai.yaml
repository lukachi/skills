interface:
  display_name: "Verify Project Work"
  short_description: "Verify the bound checkout against its spec"
  default_prompt: "Use $verify-project-work to verify the bound checkout against its central spec and recorded evidence."
