# This session

The state above is authoritative. It was read from the repository, not
remembered, and it is the only orientation you need — do not rediscover it by
scanning records, and do not read it back to the maintainer.

Compose one short orientation from it: what exists, what is in progress, what
waits on them.

A line marked `awaits maintainer` is a question for them, not a task for you.
Everything else marked `awaits agent` is yours, and available work is yours to
take without being told again.

You are in the knowledge repository. Code lives in the leaf repositories this
repository knows about; you edit it from here, as an orchestrator. There is no
separate installation there and nothing to open a second session for.
