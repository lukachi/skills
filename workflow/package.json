{
  "name": "wfctl",
  "version": "0.7.2",
  "private": false,
  "license": "MIT",
  "description": "Deterministic project workflow bootstrap and lifecycle CLI",
  "type": "module",
  "bin": {
    "wfctl": "dist/cli.js"
  },
  "files": [
    "README.md",
    "IDEA.md",
    "THIRD_PARTY.md",
    "docs",
    "spec",
    "dist",
    "evals",
    "rules",
    "skills",
    "templates",
    "vendor"
  ],
  "scripts": {
    "build": "bun scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "bun scripts/test.mjs",
    "test:preflight": "bun tests/preflight-integration.mjs",
    "test:qmd": "bun tests/qmd-integration.mjs",
    "test:graphify": "bun tests/graphify-integration.mjs",
    "test:reconstruction": "bun tests/reconstruction-integration.mjs",
    "test:runtime": "bun tests/runtime-smoke.mjs",
    "test:package": "bun tests/package-smoke.mjs",
    "check": "bun run typecheck && bun run test && bun run build && bun run test:preflight && bun run test:qmd && bun run test:graphify && bun run test:reconstruction && bun run test:runtime && bun run test:package"
  },
  "packageManager": "bun@1.3.0",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@cliffy/command": "npm:@jsr/cliffy__command@1.2.1",
    "@types/node": "^26.1.1",
    "esbuild": "^0.28.1",
    "typescript": "^7.0.2",
    "yaml": "^2.9.0"
  },
  "dependencies": {
    "@jsr/std__fmt": "1.0.10",
    "mdast-util-from-markdown": "2.0.3",
    "ora": "9.0.0",
    "skills": "1.5.20"
  }
}
