interface:
  display_name: "Process Raw Intake"
  short_description: "Adjudicate raw claims, time, and lineage"
  default_prompt: "Use $process-raw-intake to show the current frontier, classify atomic claims and chronology, verify authority, route durable outputs, and detect omissions without treating raw input as truth."
