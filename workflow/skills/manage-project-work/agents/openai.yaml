interface:
  display_name: "Manage Project Work"
  short_description: "Persist one project, leaf, or multi-repo spec"
  default_prompt: "Use $manage-project-work for a significant bounded change: maintain one central spec, bind exact checkouts, ask one material question at a time, and persist discussion, progress, verification, and knowledge promotion."
