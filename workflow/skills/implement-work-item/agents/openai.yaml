interface:
  display_name: "Implement Work Item"
  short_description: "Implement one claimed issue with checkpoints"
  default_prompt: "Use $implement-work-item to claim and implement one frontier issue in the exact bound checkout, verify it, and keep its structured checkpoint current."
policy:
  allow_implicit_invocation: false
