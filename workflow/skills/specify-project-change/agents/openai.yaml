interface:
  display_name: "Specify Project Change"
  short_description: "Synthesize one central project change specification"
  default_prompt: "Use this skill to synthesize the current discussion, project knowledge, and verified source context into the active central change specification."
policy:
  allow_implicit_invocation: false
