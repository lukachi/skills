interface:
  display_name: "Process Raw Intake"
  short_description: "Review Git-frozen raw input with QMD"
  default_prompt: "Use $process-raw-intake to inventory and adjudicate continuous raw input without treating it as truth."
